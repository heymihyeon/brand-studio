import React from 'react';
import { Container, Typography } from '@mui/material';

const Editor: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Editor Page</Typography>
    </Container>
  );
};

export default Editor;